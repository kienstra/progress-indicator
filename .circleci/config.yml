version: 2.1

orbs:
  node: circleci/node@4.1

jobs:
  lint:
    docker:
      - image: cimg/php:7.4.5-node
    steps:
      - checkout
      - node/install-packages
      - run: composer install
      - run: npm run lint

workflows:
  lint-flow:
    jobs:
      - lint
